import SwiftUI
import SwiftData

@main
struct BrainHacker1_2App: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .modelContainer(for: [CellItem.self])
    }
}
